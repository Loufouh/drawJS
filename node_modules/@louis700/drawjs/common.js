"use strict";

export let targetContext;

/**
 * Target a canvas' context (so you won't have to specify it everywhere).
 * @param ctx The canvas' context.
 */
export function setTargetContext(ctx) {
    targetContext = ctx;
}
